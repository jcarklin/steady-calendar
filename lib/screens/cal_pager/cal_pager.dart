import 'package:flutter/material.dart';

class CalPager extends StatelessWidget {

  static const routeName = '/cal_pager';

  const CalPager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

}